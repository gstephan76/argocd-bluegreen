apiVersion: v1
kind: Secret
metadata:
  name: metric-ai-github-bootstrap
  namespace: openshift-gitops
type: Opaque
stringData:
  # The upstream agent requires github.token to be non-empty at startup.
  # This is deliberately NOT a credential and grants no GitHub access.
  github_token: metric-ai-remediation-disabled
